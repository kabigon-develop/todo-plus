---
phase: 01-token
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/style.css
autonomous: true
requirements:
  - TOKEN-01
  - TOKEN-04

must_haves:
  truths:
    - "src/style.css 的 :root 块包含所有语义化颜色 token（surface、text、primary、border、priority）"
    - "src/style.css 的 .dark 块用 stone-900 系暖灰覆盖深色值，主色覆盖为 teal-400"
    - "浅色主色 #14b8a6（teal-500），深色主色 #2dd4bf（teal-400），符合 WCAG AA"
    - "shadcn 兼容别名 --background 已定义，防止 Button ring-offset-background 失效"
  artifacts:
    - path: "src/style.css"
      provides: "完整 CSS 变量 Token 体系（:root 浅色 + .dark 深色）"
      contains: "--surface-base"
  key_links:
    - from: "src/style.css :root"
      to: ".dark"
      via: "CSS Cascade 覆盖"
      pattern: "\\.dark\\s*\\{"
---

<objective>
在 src/style.css 中建立完整的 CSS 自定义属性设计 Token 体系。

Purpose: 为整个阶段 1 奠定 Token 基础。tailwind.config.js 中的 var() 引用、组件类名替换，都依赖此文件的变量定义先存在。
Output: src/style.css 包含完整的 :root（浅色）和 .dark（深色）CSS 变量块，body 使用语义化类。
</objective>

<execution_context>
@/Users/kabigon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabigon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-token/01-CONTEXT.md
@.planning/phases/01-token/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 在 style.css 写入完整 Token 体系</name>
  <files>src/style.css</files>
  <action>
替换 src/style.css 全部内容为以下结构（保留原 3 行 Tailwind 指令，追加变量块）：

1. 保留开头三行：
   @tailwind base;
   @tailwind components;
   @tailwind utilities;

2. 追加 :root 块（浅色值）：
   - Surface 3 层：
     --surface-base: #f8fafc（slate-50，页面背景）
     --surface-card: #ffffff（white，卡片/内容区）
     --surface-elevated: #ffffff（white，弹层/浮层）
   - 文字 3 档：
     --text-foreground: #1f2937（gray-800，主文字）
     --text-muted: #6b7280（gray-500，次要信息）
     --text-subtle: #9ca3af（gray-400，占位符/辅助）
   - 主色 teal-500 系：
     --color-primary: #14b8a6（teal-500）
     --color-primary-hover: #0d9488（teal-600）
     --color-primary-muted: #ccfbf1（teal-100）
     --color-primary-fg: #ffffff（主色背景上的文字色）
     --color-primary-text: #0f766e（teal-700，用于主色作文字时符合 WCAG AA）
   - 边框：
     --border-default: #e2e8f0（slate-200）
     --border-focus: #14b8a6（primary 色）
   - 优先级：
     --priority-high: #ef4444（red-500）
     --priority-medium: #f59e0b（amber-500）
     --priority-low: #22c55e（green-500）
   - 叠加层：
     --overlay: rgba(15, 15, 15, 0.5)
   - shadcn 兼容别名（防止 Button ring-offset-background 失效）：
     --background: var(--surface-card)

3. 追加 .dark 块（深色覆盖，stone-900 暖调系）：
   - Surface 3 层：
     --surface-base: #1c1917（stone-900）
     --surface-card: #292524（stone-800）
     --surface-elevated: #3c3634（stone-700）
   - 文字 3 档：
     --text-foreground: #f5f5f4（stone-100）
     --text-muted: #a8a29e（stone-400）
     --text-subtle: #78716c（stone-500）
   - 主色 teal-400 系（深色下对比度更高）：
     --color-primary: #2dd4bf（teal-400）
     --color-primary-hover: #14b8a6（teal-500）
     --color-primary-muted: #134e4a（teal-900）
     --color-primary-fg: #042f2e（teal-900 深色文字，对比度约 8:1，优于纯黑且与青绿系一致）
     --color-primary-text: #2dd4bf（teal-400 作文字色）
   - 边框：
     --border-default: #44403c（stone-700）
     --border-focus: #2dd4bf
   - 优先级保持不变（red/amber/green 在深色背景下无需调整）：
     --priority-high: #ef4444
     --priority-medium: #f59e0b
     --priority-low: #22c55e
   - 叠加层：
     --overlay: rgba(0, 0, 0, 0.6)
   - shadcn 兼容别名：
     --background: var(--surface-card)

4. 更新 body 样式（替换原 bg-mist text-ink 为语义化类）：
   body {
     @apply bg-surface-base text-foreground;
   }

注意：此时 bg-surface-base 和 text-foreground 尚未在 tailwind.config.js 中注册，body 样式先写在这里，01-02 完成 tailwind.config.js 更新后才会生效。这是正确的顺序（先定义变量，再注册为工具类）。
  </action>
  <verify>
    <automated>grep -c "surface-base\|surface-card\|text-foreground\|color-primary\|border-default\|priority-high" /Users/kabigon/project/todo-plus/src/style.css</automated>
  </verify>
  <done>
    - src/style.css 含有 :root 块和 .dark 块
    - grep 输出显示至少 6 个匹配（每个核心 token 组至少一处）
    - --background 别名存在于两个块中
    - body 使用 @apply bg-surface-base text-foreground
  </done>
</task>

</tasks>

<verification>
运行以下检查确认 Token 定义完整：

```bash
# 检查浅色 token 存在
grep ":root" /Users/kabigon/project/todo-plus/src/style.css

# 检查深色覆盖存在
grep "\.dark" /Users/kabigon/project/todo-plus/src/style.css

# 检查关键 token 存在（应输出非 0）
grep -c "\-\-color-primary\|\-\-surface-base\|\-\-text-foreground\|\-\-border-default\|\-\-priority-high\|\-\-background" /Users/kabigon/project/todo-plus/src/style.css
```
</verification>

<success_criteria>
- src/style.css 包含 :root 和 .dark 两个 CSS 变量块
- 所有语义化 token 定义完整：surface（3 层）、text（3 档）、primary（5 个变体）、border（2 个）、priority（3 个）、overlay（1 个）
- shadcn 兼容别名 --background 在两个块中均有定义
- 浅色 primary 为 #14b8a6（teal-500），深色 primary 为 #2dd4bf（teal-400）
- body 已更新为 @apply bg-surface-base text-foreground
</success_criteria>

<output>
完成后创建 .planning/phases/01-token/01-01-SUMMARY.md，记录：
- 已定义的 token 列表和值
- 任何调整说明
- 下一步提示（01-02 依赖此文件的变量定义）
</output>
