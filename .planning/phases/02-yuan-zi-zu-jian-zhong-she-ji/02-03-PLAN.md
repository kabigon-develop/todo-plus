---
phase: 02-yuan-zi-zu-jian-zhong-she-ji
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified: []
autonomous: false
requirements:
  - COMP-01
  - COMP-02
  - COMP-03
  - COMP-04
  - COMP-05

must_haves:
  truths:
    - "用户在浏览器中可见：Button teal 主色背景 + 白色加粗文字（浅色/深色均正确）"
    - "用户可见：Badge priority 变体红/琥珀/青绿淡色，主题切换后颜色随之变化"
    - "用户可见：Tab 底部 teal 指示线随点击平滑切换"
    - "用户可见：Dialog 从底部滑入 + 毛玻璃背景，关闭时退出"
    - "用户可见：Input 聚焦时 teal 光环，error 状态时红色边框"
  artifacts: []
  key_links: []
---

<objective>
人工视觉验证 Phase 2 全部组件改造效果。所有自动化测试已在 Plan 01/02 完成，此计划为最终视觉确认关卡，确保深色/浅色主题下所有组件的视觉效果符合设计决策。

Purpose: 在提交阶段完成前，由用户确认真实浏览器渲染结果无问题。
Output: 用户批准 → Phase 2 标记为 Complete。
</objective>

<execution_context>
@/Users/kabigon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabigon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-01-SUMMARY.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>视觉验证：Phase 2 全部组件（COMP-01 ~ COMP-05）</name>
  <action>
    在浏览器中逐一验证以下 5 个组件的视觉效果。自动化测试已在 Plan 01/02 通过，本步骤为最终视觉确认。
    先运行 `npx vitest run` 确认全量套件通过，再启动 `npm run dev` 开始视觉检查。
  </action>
  <what-built>
    Phase 2 原子组件重设计全部完成：
    - COMP-01: Button default(teal) + outline(teal 边框) 迁移
    - COMP-02: Badge priority 变体(high/medium/low) + 状态变体 Token 迁移
    - COMP-03: Tab 底部指示器动效（CSS 伪元素，200ms opacity 过渡）
    - COMP-04: Dialog 底部滑入（200ms）+ 毛玻璃背景遮罩
    - COMP-05: Input error prop + Select focus Token 验证
    - Checkbox: checked 态 teal 填充
  </what-built>
  <how-to-verify>
    1. 先运行自动化测试：`npx vitest run`（确认全绿）
    2. 启动开发服务器：`npm run dev`，打开 http://localhost:5173

    **Button（COMP-01）：**
    - 检查主操作按钮（如"新增 Todo"）是否显示 teal（#14b8a6）背景色 + 白色加粗文字
    - 切换深色主题，确认 Button 背景色变为 teal-400（#2dd4bf），文字仍白色
    - 确认 outline 变体（若有）显示 teal 边框 + teal-700 文字

    **Badge（COMP-02）：**
    - 检查 Todo 卡片上优先级标签（高/中/低）分别显示红/琥珀/青绿淡色背景
    - 切换主题，确认颜色随 CSS Token 变化（深色下文字更亮）
    - 开发者工具确认无 emerald-100、amber-100 等硬编码类残留

    **Tab 指示器（COMP-03）：**
    - 点击三个 Tab，观察活动 Tab 底部出现 teal 细线（约 200ms opacity 过渡）
    - 活动 Tab 文字加粗 + teal-700 颜色；非活动 Tab muted 灰色，无底部线条

    **Dialog（COMP-04）：**
    - 点击"新增"或"编辑"按钮打开对话框
    - 确认弹层从屏幕底部向上滑入（约 200ms），背景出现毛玻璃虚化
    - 关闭时弹层向下退出（约 150ms），毛玻璃消失
    - 深色模式下重复确认

    **Input/Checkbox（COMP-05）：**
    - 点击输入框，获取焦点时出现 teal 色 ring（光环）
    - 勾选 Todo Checkbox，确认变为 teal 填充 + 白色勾选图标
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>用户确认所有 5 个组件在浅色和深色主题下视觉均符合 CONTEXT.md 中的设计决策，且 npx vitest run 全量通过。</done>
  <resume-signal>输入 "approved" 确认所有组件视觉正确，或描述发现的问题（Claude 将修复后重新提交验证）。</resume-signal>
</task>

</tasks>

<verification>
用户输入 "approved" 后，Plan 03 完成，Phase 2 可标记为 Complete。
</verification>

<success_criteria>
- 用户确认所有 5 个组件（Button/Badge/Tab/Dialog/Input）在浅色和深色主题下视觉均符合 CONTEXT.md 中的设计决策
- npx vitest run 全量通过
- Phase 2 ROADMAP 状态更新为 Complete
</success_criteria>

<output>
完成后创建 `.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-03-SUMMARY.md`
</output>
