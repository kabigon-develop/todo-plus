---
phase: 02-yuan-zi-zu-jian-zhong-she-ji
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button/Button.vue
  - src/components/ui/badge/Badge.vue
  - src/components/ui/input/Input.vue
  - src/components/ui/checkbox/Checkbox.vue
  - src/style.css
  - tailwind.config.js
  - tests/components/button.test.ts
  - tests/components/badge.test.ts
  - tests/components/input.test.ts
autonomous: true
requirements:
  - COMP-01
  - COMP-02
  - COMP-05

must_haves:
  truths:
    - "Button default variant 在浅色和深色主题下均显示 teal-500 背景色 + 白色文字"
    - "Button outline variant 显示 teal 边框 + teal-700 文字（primary-text）"
    - "Badge priority 变体（high/medium/low）分别显示红/琥珀/青绿淡色背景 + 对应深色文字"
    - "Badge 状态变体（warning/success/destructive/info）不再含 hardcoded Tailwind 调色板类"
    - "Input focus 态显示 ring-border-focus（teal 环）；error prop 传入时显示红色边框"
    - "Checkbox 勾选态背景变为 bg-primary（teal），白色勾选图标可见"
  artifacts:
    - path: "src/components/ui/button/Button.vue"
      provides: "Button CVA，default 变体含 bg-primary text-white font-semibold hover:bg-primary-hover"
      contains: "bg-primary"
    - path: "src/components/ui/badge/Badge.vue"
      provides: "Badge CVA，含 high/medium/low priority 变体 + 迁移后的状态变体"
      contains: "priority-high-bg"
    - path: "src/components/ui/input/Input.vue"
      provides: "Input，含 error prop，错误态显示 border-red-400"
      contains: "error"
    - path: "src/components/ui/checkbox/Checkbox.vue"
      provides: "Checkbox，checked 态含 bg-primary border-primary text-white"
      contains: "data-[state=checked]:bg-primary"
    - path: "src/style.css"
      provides: "priority soft-bg CSS Token（--priority-high-bg 等）"
      contains: "--priority-high-bg"
    - path: "tests/components/button.test.ts"
      provides: "COMP-01 Button CVA 类名断言"
    - path: "tests/components/badge.test.ts"
      provides: "COMP-02 Badge priority 变体断言"
    - path: "tests/components/input.test.ts"
      provides: "COMP-05 Input error/focus 类名断言"
  key_links:
    - from: "src/style.css"
      to: "src/components/ui/badge/Badge.vue"
      via: "CSS arbitrary value bg-[--priority-high-bg]"
      pattern: "priority-high-bg"
    - from: "tailwind.config.js"
      to: "src/components/ui/button/Button.vue"
      via: "bg-primary Tailwind 工具类"
      pattern: "primary.*DEFAULT.*var\\(--color-primary\\)"
---

<objective>
将 Button、Badge、Input、Checkbox 四个组件迁移至语义化 CSS Token，消除所有硬编码 Tailwind 调色板颜色，新增 Badge priority 变体和 Input error 态。同时建立 Wave 0 测试基础设施（CVA 函数提取 + 测试骨架 + tailwindcss-animate 注册）。

Purpose: 完成 COMP-01/02/05 需求，建立可在 node 环境运行的 CVA 类名断言测试。
Output: 四个迁移后的组件文件、更新的 style.css（priority soft-bg token）、三个测试文件。
</objective>

<execution_context>
@/Users/kabigon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabigon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-CONTEXT.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-RESEARCH.md

<interfaces>
<!-- 当前组件关键接口 — executor 直接使用，无需探索代码库 -->

Button.vue — 当前 CVA default/outline variant（需替换）:
```typescript
// 当前（待替换）
default: 'bg-foreground text-surface-base hover:bg-foreground/90',
outline: 'border border-border bg-surface-card hover:bg-surface-base',
// 替换后（锁定决策）
default: 'bg-primary text-white font-semibold hover:bg-primary-hover',
outline: 'border border-primary bg-surface-card text-primary-text hover:bg-primary-muted',
```

Badge.vue — 当前状态变体（需迁移）:
```typescript
// 当前（硬编码，待迁移）
success:     'border-transparent bg-emerald-100 text-emerald-700',
warning:     'border-transparent bg-amber-100 text-amber-700',
destructive: 'border-transparent bg-red-100 text-red-700',
info:        'border-transparent bg-sky-100 text-sky-700',
// 迁移后（使用 CSS Token）
success:     'border-transparent bg-[--priority-low-bg] text-[--priority-low-text]',
warning:     'border-transparent bg-[--priority-medium-bg] text-[--priority-medium-text]',
destructive: 'border-transparent bg-[--priority-high-bg] text-[--priority-high-text]',
info:        'border-transparent bg-primary-muted text-primary-text',
// 新增 priority 变体
high:   'border-transparent bg-[--priority-high-bg] text-[--priority-high-text]',
medium: 'border-transparent bg-[--priority-medium-bg] text-[--priority-medium-text]',
low:    'border-transparent bg-[--priority-low-bg] text-[--priority-low-text]',
```

Checkbox.vue — checked 态（需替换）:
```
// 当前: data-[state=checked]:border-foreground data-[state=checked]:bg-foreground data-[state=checked]:text-surface-base
// 替换: data-[state=checked]:bg-primary data-[state=checked]:border-primary data-[state=checked]:text-white
```

Input.vue — 当前无 error prop（需新增）:
```typescript
// 当前 defineProps: { class?: HTMLAttributes['class'] }
// 新增: error?: boolean
// error 为 true 时: border-red-400 focus-visible:ring-red-400 替换 border-border focus-visible:ring-border-focus
```

style.css Priority soft-bg Token（需新增到 :root 和 .dark）:
```css
:root {
  --priority-high-bg:    rgba(239, 68, 68, 0.12);
  --priority-high-text:  #dc2626;
  --priority-medium-bg:  rgba(245, 158, 11, 0.12);
  --priority-medium-text: #d97706;
  --priority-low-bg:     rgba(34, 197, 94, 0.12);
  --priority-low-text:   #16a34a;
}
.dark {
  --priority-high-bg:    rgba(239, 68, 68, 0.18);
  --priority-high-text:  #f87171;
  --priority-medium-bg:  rgba(245, 158, 11, 0.18);
  --priority-medium-text: #fbbf24;
  --priority-low-bg:     rgba(34, 197, 94, 0.18);
  --priority-low-text:   #4ade80;
}
```

tailwind.config.js — 当前 plugins（需新增 tailwindcss-animate）:
```javascript
// 当前: plugins: [forms]
// 新增: plugins: [forms, require('tailwindcss-animate')]
// 注意: 需先 import 或 require，当前文件使用 ESM import 语法
// ESM 写法: import animate from 'tailwindcss-animate'; plugins: [forms, animate]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 基础设施 — CVA 函数提取 + 测试骨架 + tailwindcss-animate 注册</name>
  <files>
    src/components/ui/button/index.ts,
    src/components/ui/badge/index.ts,
    src/components/ui/input/index.ts,
    tests/components/button.test.ts,
    tests/components/badge.test.ts,
    tests/components/input.test.ts,
    tailwind.config.js
  </files>
  <behavior>
    - button.test.ts COMP-01: buttonVariants({ variant: 'default' }) 包含 'bg-primary' 且不含 'bg-foreground'
    - button.test.ts COMP-01: buttonVariants({ variant: 'outline' }) 包含 'border-primary' 和 'text-primary-text'
    - badge.test.ts COMP-02: badgeVariants({ variant: 'high' }) 包含 '--priority-high-bg' 和 '--priority-high-text'
    - badge.test.ts COMP-02: badgeVariants({ variant: 'medium' }) 包含 '--priority-medium-bg'
    - badge.test.ts COMP-02: badgeVariants({ variant: 'low' }) 包含 '--priority-low-bg'
    - badge.test.ts COMP-02: badgeVariants({ variant: 'success' }) 不含 'emerald'
    - input.test.ts COMP-05: inputVariants 或 getInputClass({ error: true }) 包含 'border-red-400'
    - input.test.ts COMP-05: inputVariants 或 getInputClass({ error: false }) 包含 'ring-border-focus'
  </behavior>
  <action>
    步骤 1 — tailwind.config.js 注册 tailwindcss-animate：
    当前文件使用 ESM（import forms from '@tailwindcss/forms'），在顶部加入 `import animate from 'tailwindcss-animate'`，并在 plugins 数组改为 `[forms, animate]`。

    步骤 2 — 提取 CVA 函数为独立模块（供 node 环境测试导入）：
    - `src/components/ui/button/index.ts`：从 Button.vue 的 script 段提取 `buttonVariants` 函数并导出。内容包含 cva 调用（和 Button.vue 完全相同的类字符串），暂时先写 placeholder 类（留给 Task 2 替换真实值后测试才会绿），即先写与 Button.vue 当前一致的旧值，测试写成断言新值，Task 2 改组件时同步更新 index.ts。
    - `src/components/ui/badge/index.ts`：同上提取 `badgeVariants`（含原状态变体 + 新 priority 变体占位）
    - `src/components/ui/input/index.ts`：导出 `getInputClass(opts: { error?: boolean; class?: string }): string` 函数，用 cn() 拼接 Input 的类名逻辑

    步骤 3 — 创建失败测试（RED 阶段）：
    在 `tests/components/` 目录创建三个测试文件，断言上述 behavior 中描述的新值（当前组件未迁移，测试应该失败）。测试使用 `import { buttonVariants } from '../../src/components/ui/button/index'` 等路径，在 node 环境运行，无需 mount 组件。

    步骤 4 — 验证测试确实失败（RED 确认）：运行 `npx vitest run tests/components/`，确认三个文件均有失败项。
  </action>
  <verify>
    <automated>npx vitest run tests/components/ 2>&1 | grep -E "FAIL|PASS|failed|passed"</automated>
  </verify>
  <done>三个测试文件存在，测试在 RED 状态（断言新值，组件未迁移故失败）。tailwind.config.js plugins 包含 animate。index.ts 模块可在 node 环境被 import。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Button + Badge + Checkbox 组件迁移（COMP-01、COMP-02 部分）</name>
  <files>
    src/components/ui/button/Button.vue,
    src/components/ui/button/index.ts,
    src/components/ui/badge/Badge.vue,
    src/components/ui/badge/index.ts,
    src/components/ui/checkbox/Checkbox.vue,
    src/style.css
  </files>
  <behavior>
    - Button default: 含 bg-primary text-white font-semibold hover:bg-primary-hover，不含 bg-foreground
    - Button outline: 含 border-primary text-primary-text，不含 border-border（outline 专用，去掉原来无前缀的 border-border）
    - Badge high/medium/low variant 存在且含对应 CSS arbitrary value
    - Badge success/warning/destructive/info 不含 emerald/amber/red-100/sky-100 等硬编码调色板类
    - Checkbox data-[state=checked]: 含 bg-primary border-primary text-white，不含 bg-foreground
  </behavior>
  <action>
    步骤 1 — 在 src/style.css 的 :root 块末尾追加 priority soft-bg token（6 个 :root 变量 + 6 个 .dark 变量，值见 interfaces 块）。

    步骤 2 — 修改 Button.vue CVA：
    - `default` 变体替换为 `'bg-primary text-white font-semibold hover:bg-primary-hover'`
    - `outline` 变体替换为 `'border border-primary bg-surface-card text-primary-text hover:bg-primary-muted'`
    - 同步更新 `src/components/ui/button/index.ts` 中的 buttonVariants 为相同新值

    步骤 3 — 修改 Badge.vue CVA：
    - 迁移 4 个状态变体（success/warning/destructive/info），使用 CSS arbitrary value 语法（见 interfaces）
    - 新增 high/medium/low priority 变体（见 interfaces）
    - 同步更新 `src/components/ui/badge/index.ts` 中的 badgeVariants

    步骤 4 — 修改 Checkbox.vue：将 `data-[state=checked]:border-foreground data-[state=checked]:bg-foreground data-[state=checked]:text-surface-base` 替换为 `data-[state=checked]:bg-primary data-[state=checked]:border-primary data-[state=checked]:text-white`。可选加入 `transition-colors duration-150` 使勾选有过渡感（Claude's Discretion）。

    步骤 5 — 运行测试（GREEN 验证）：button.test.ts 和 badge.test.ts 必须全绿。
  </action>
  <verify>
    <automated>npx vitest run tests/components/button.test.ts tests/components/badge.test.ts</automated>
  </verify>
  <done>button.test.ts 和 badge.test.ts 全绿。Button.vue/Badge.vue/Checkbox.vue 代码中无 bg-foreground（default variant）、无 emerald-100/amber-100/red-100/sky-100 硬编码类。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Input + Select 迁移（COMP-05）</name>
  <files>
    src/components/ui/input/Input.vue,
    src/components/ui/input/index.ts,
    src/components/ui/select/Select.vue
  </files>
  <behavior>
    - Input 无 error prop 时：含 border-border 和 focus-visible:ring-border-focus
    - Input error=true 时：含 border-red-400 和 focus-visible:ring-red-400，不含 border-border
    - Select trigger 已含 focus:ring-border-focus（验证无遗漏，无需修改）
  </behavior>
  <action>
    步骤 1 — 修改 Input.vue：
    - 在 defineProps 中新增 `error?: boolean` prop
    - 将 :class 绑定改为条件逻辑：正常态保留 `border-border focus-visible:ring-border-focus focus-visible:ring-border`；error 态改为 `border-red-400 focus-visible:ring-red-400`
    - 具体实现用 cn() 条件分支：`cn('flex h-10 w-full rounded-md border bg-surface-card px-3 py-2 text-sm text-foreground ring-offset-surface-card file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-subtle focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 disabled:cursor-not-allowed disabled:opacity-50', props.error ? 'border-red-400 focus-visible:ring-red-400' : 'border-border focus-visible:ring-border-focus', $props.class)`
    - 同步更新 input/index.ts 中的 getInputClass 函数

    步骤 2 — 验证 Select.vue：确认 SelectTrigger 已含 `focus:ring-border-focus`（源码第 41 行确认已有），无需修改。若发现遗漏则补充。

    步骤 3 — 运行全部测试（GREEN）：三个测试文件必须全绿。
  </action>
  <verify>
    <automated>npx vitest run tests/components/</automated>
  </verify>
  <done>tests/components/ 下三个文件全绿。Input.vue 含 error prop，Select.vue focus token 已确认无遗漏。npx vitest run 全局套件通过。</done>
</task>

</tasks>

<verification>
npx vitest run 全量通过（包含历史测试）。

在浏览器中确认：
1. Button default 显示 teal 背景色，白色加粗文字（浅色/深色主题均正确）
2. Badge 优先级变体颜色正确（红/琥珀/青绿淡色）
3. Checkbox 勾选后变为 teal 填充
4. Input focus 时出现 teal 环；测试 error prop 时出现红色边框
</verification>

<success_criteria>
- COMP-01: Button.vue default variant = `bg-primary text-white font-semibold hover:bg-primary-hover`；outline variant 含 `border-primary text-primary-text`
- COMP-02: Badge.vue 含 high/medium/low 三个 priority 变体，所有状态变体无硬编码调色板类
- COMP-05: Input.vue 含 error prop，Select.vue focus token 已验证
- 测试: button.test.ts、badge.test.ts、input.test.ts 全绿
- 无回归：npx vitest run 全局通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-01-SUMMARY.md`
</output>
