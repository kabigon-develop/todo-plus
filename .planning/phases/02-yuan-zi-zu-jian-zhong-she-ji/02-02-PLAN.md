---
phase: 02-yuan-zi-zu-jian-zhong-she-ji
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/components/ui/tabs/TabsTrigger.vue
  - src/components/ui/tabs/TabsList.vue
  - src/components/ui/dialog/DialogContent.vue
  - src/style.css
  - tailwind.config.js
  - tests/components/tabs.test.ts
  - tests/components/dialog.test.ts
autonomous: true
requirements:
  - COMP-03
  - COMP-04

must_haves:
  truths:
    - "点击不同 Tab 后，活动 Tab 底部出现 teal 色底部指示线，文字变为 teal-700 + 加粗"
    - "非活动 Tab 无底部线条，文字为 muted 色"
    - "打开 Dialog 时，弹层从屏幕底部向上滑入（200ms ease-out），背景出现毛玻璃虚化"
    - "关闭 Dialog 时，弹层向下滑出（150ms），毛玻璃背景淡出"
    - "Tab 和 Dialog 动画均通过 data-[state=*] CSS 驱动，无 Vue <Transition> 包裹"
  artifacts:
    - path: "src/components/ui/tabs/TabsTrigger.vue"
      provides: "Tab 指示器：after: 伪元素 + data-[state=active]:after:opacity-100"
      contains: "after:bg-primary"
    - path: "src/components/ui/dialog/DialogContent.vue"
      provides: "Dialog 底部滑入：bottom-0 定位 + animate-in slide-in-from-bottom"
      contains: "slide-in-from-bottom"
    - path: "tests/components/tabs.test.ts"
      provides: "COMP-03 Tab 指示器类名断言"
    - path: "tests/components/dialog.test.ts"
      provides: "COMP-04 Dialog 动效类名断言"
  key_links:
    - from: "tailwind.config.js"
      to: "src/components/ui/dialog/DialogContent.vue"
      via: "tailwindcss-animate 插件注册（animate-in, slide-in-from-bottom）"
      pattern: "tailwindcss-animate"
    - from: "src/components/ui/tabs/TabsTrigger.vue"
      to: "src/style.css（间接）"
      via: "after:bg-primary → --color-primary CSS Token"
      pattern: "after:bg-primary"
---

<objective>
重设计 Tab 导航（CSS 伪元素底部指示器）和 Dialog 弹层（底部滑入 + 毛玻璃背景）。这是 Phase 2 中唯一涉及动效的计划，依赖 02-01 完成（tailwindcss-animate 已在 Plan 01 注册）。

Purpose: 完成 COMP-03/04 需求，叠加两处视觉动效，同时保持 Radix Vue data-state 驱动模式。
Output: 更新后的 TabsTrigger.vue、DialogContent.vue，以及两个测试文件。
</objective>

<execution_context>
@/Users/kabigon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kabigon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-CONTEXT.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-RESEARCH.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-se-ji/02-01-SUMMARY.md

<interfaces>
<!-- 当前组件接口 — executor 直接使用 -->

TabsTrigger.vue — 当前类名（需替换）:
```
当前: 'inline-flex items-center justify-center whitespace-nowrap rounded-sm px-3 py-1.5 text-sm font-medium text-muted ring-offset-surface-card transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-border-focus focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 data-[state=active]:bg-surface-card data-[state=active]:text-foreground data-[state=active]:shadow-sm'

替换为（完整新类串）:
'relative inline-flex items-center justify-center whitespace-nowrap rounded-none px-3 py-1.5 text-sm font-medium text-muted ring-offset-surface-card transition-all focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-border-focus focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50 after:absolute after:bottom-0 after:left-0 after:right-0 after:h-0.5 after:content-[\'\'] after:rounded-full after:bg-primary after:opacity-0 after:transition-opacity after:duration-200 data-[state=active]:text-primary-text data-[state=active]:font-semibold data-[state=active]:after:opacity-100'
```
注意：移除 data-[state=active]:bg-surface-card 和 data-[state=active]:shadow-sm（胶囊背景取消），改为底部线条。TabsList 的 bg-surface-base p-1 胶囊背景保留不变。
rounded-none 替换原来的 rounded-sm（活动态不再有背景色块需要圆角）。

TabsList.vue — 保持不变:
```
'inline-flex h-10 items-center justify-center rounded-md bg-surface-base p-1 text-muted'
```
保留胶囊形外框背景，不修改。

DialogContent.vue — 当前（需改造）:
```html
<!-- 当前 DialogOverlay -->
<DialogOverlay class="fixed inset-0 z-50 bg-[--overlay]" />

<!-- 当前 DialogContent -->
<DialogContent
  :class="cn('fixed left-1/2 top-1/2 z-50 w-[95vw] max-w-lg -translate-x-1/2 -translate-y-1/2 rounded-lg border border-border bg-surface-elevated p-6 shadow-lg ring-offset-surface-card', props.class)"
>
```

改造后（使用 tailwindcss-animate 的 animate-in/out 工具类）:
```html
<!-- DialogOverlay 加 backdrop-blur + 动效 -->
<DialogOverlay
  class="fixed inset-0 z-50 bg-[--overlay] backdrop-blur-md
    data-[state=open]:animate-in data-[state=open]:fade-in-0
    data-[state=closed]:animate-out data-[state=closed]:fade-out-0
    data-[state=closed]:duration-150"
/>

<!-- DialogContent 改为底部定位 + 滑入动效 -->
<DialogContent
  v-bind="forwarded"
  :class="cn(
    'fixed bottom-0 left-1/2 z-50 w-[95vw] max-w-lg -translate-x-1/2',
    'rounded-t-xl border-t border-border bg-surface-elevated p-6 shadow-lg ring-offset-surface-card',
    'data-[state=open]:animate-in data-[state=open]:slide-in-from-bottom data-[state=open]:duration-200',
    'data-[state=closed]:animate-out data-[state=closed]:slide-out-to-bottom data-[state=closed]:duration-150',
    props.class
  )"
>
```
注意：
- 移除 top-1/2 -translate-y-1/2，改为 bottom-0（否则 translateY 动画方向错误）
- 圆角从 rounded-lg 改为 rounded-t-xl（仅顶部圆角，底部紧贴屏幕边缘）
- border-border 改为 border-t border-border（仅顶部边框）
- tailwindcss-animate 类在 Plan 01 Task 1 已注册插件，此处直接使用
- 若 tailwindcss-animate 的 slide-in-from-bottom 不可用，降级使用手写 keyframes（在 style.css 定义 slideUp/slideDown，在 tailwind.config.js 注册动画名）

CVA 函数提取模式（测试用）:
```typescript
// tests/components/tabs.test.ts — 策略：直接测试 TabsTrigger 的 :class 字符串包含关键类
// 由于 TabsTrigger 不用 CVA，测试方案改为：
// 直接导出 tabsTriggerClass 常量或函数（在 tests/ 中 hardcode 期望的类子串）
// 测试断言字符串片段，而非 import 组件函数

// 最简方案：在 TabsTrigger.vue 旁边新建 tabs/index.ts
// 导出 getTriggerClass(): string，复用组件内的 cn(...) 逻辑

// tests/components/dialog.test.ts — 同理，新建 dialog/index.ts 导出 getDialogContentClass() 和 getDialogOverlayClass()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tab 导航指示器重设计（COMP-03）</name>
  <files>
    src/components/ui/tabs/TabsTrigger.vue,
    src/components/ui/tabs/index.ts,
    tests/components/tabs.test.ts
  </files>
  <behavior>
    - getTriggerClass() 返回的字符串包含 'after:bg-primary'
    - getTriggerClass() 返回的字符串包含 'data-[state=active]:after:opacity-100'
    - getTriggerClass() 返回的字符串包含 'data-[state=active]:text-primary-text'
    - getTriggerClass() 返回的字符串包含 'data-[state=active]:font-semibold'
    - getTriggerClass() 返回的字符串不包含 'data-[state=active]:bg-surface-card'（旧胶囊背景已移除）
    - getTriggerClass() 返回的字符串包含 'after:opacity-0'（默认隐藏指示线）
  </behavior>
  <action>
    步骤 1 — 创建 RED 测试（先写 tests/components/tabs.test.ts）：
    新建 `src/components/ui/tabs/index.ts`，导出 `getTriggerClass(): string` 函数（返回当前旧类名字符串作为占位）。
    在 tests/components/tabs.test.ts 中，import { getTriggerClass } from '../../src/components/ui/tabs/index' 并断言上述 behavior（此时旧类字符串不含 after:bg-primary，测试失败）。
    运行 npx vitest run tests/components/tabs.test.ts 确认 RED。

    步骤 2 — 修改 TabsTrigger.vue（GREEN）：
    将 :class 的 cn() 调用替换为 interfaces 中描述的新类串：
    - 移除 rounded-sm，改为 rounded-none
    - 移除 data-[state=active]:bg-surface-card data-[state=active]:text-foreground data-[state=active]:shadow-sm
    - 新增 relative + after: 伪元素系列类（见 interfaces 完整字符串）
    - 新增 data-[state=active]:text-primary-text data-[state=active]:font-semibold data-[state=active]:after:opacity-100

    步骤 3 — 同步更新 `src/components/ui/tabs/index.ts` 的 getTriggerClass() 返回值为新类串。

    步骤 4 — 运行测试验证 GREEN：npx vitest run tests/components/tabs.test.ts

    TabsList.vue 不修改（保留胶囊形背景）。
  </action>
  <verify>
    <automated>npx vitest run tests/components/tabs.test.ts</automated>
  </verify>
  <done>tabs.test.ts 全绿。TabsTrigger.vue 含 after:bg-primary 和 data-[state=active]:after:opacity-100，不含 data-[state=active]:bg-surface-card。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dialog 弹层重设计（COMP-04）</name>
  <files>
    src/components/ui/dialog/DialogContent.vue,
    src/components/ui/dialog/index.ts,
    tests/components/dialog.test.ts
  </files>
  <behavior>
    - getDialogContentClass() 包含 'bottom-0'（底部定位）
    - getDialogContentClass() 包含 'rounded-t-xl'（仅顶部圆角）
    - getDialogContentClass() 包含 'slide-in-from-bottom'
    - getDialogContentClass() 包含 'slide-out-to-bottom'
    - getDialogContentClass() 不包含 'top-1/2'（旧居中定位已移除）
    - getDialogContentClass() 不包含 '-translate-y-1/2'（旧垂直位移已移除）
    - getDialogOverlayClass() 包含 'backdrop-blur-md'
    - getDialogOverlayClass() 包含 'fade-in-0'
  </behavior>
  <action>
    步骤 1 — 确认 tailwindcss-animate 可用：
    运行 `npx tailwindcss -i src/style.css -o /dev/null 2>&1 | head -5` 确认无插件报错。若 tailwindcss-animate 未正确注册（Plan 01 中应已完成），在此重新确认 tailwind.config.js plugins 包含 animate。

    步骤 2 — 创建 RED 测试：
    新建 `src/components/ui/dialog/index.ts`，导出 `getDialogContentClass(): string` 和 `getDialogOverlayClass(): string`，初始返回旧类字符串。
    创建 tests/components/dialog.test.ts，断言上述 behavior。运行确认 RED。

    步骤 3 — 修改 DialogContent.vue（GREEN）：
    按 interfaces 中的改造后代码修改 DialogOverlay 和 DialogContent：
    - DialogOverlay：新增 backdrop-blur-md + animate-in/out fade 类
    - DialogContent：定位改为 bottom-0，移除 top-1/2 -translate-y-1/2，改为 rounded-t-xl border-t，新增 animate-in/out slide 类

    若 tailwindcss-animate 的 slide-in-from-bottom 类不生效（fallback）：
    在 style.css 追加 @keyframes slideUp/slideDown（见 RESEARCH.md Pattern 2），在 tailwind.config.js theme.extend.animation + keyframes 中注册（见 RESEARCH.md Pattern 3），然后改用 data-[state=open]:animate-slideUp 代替。

    步骤 4 — 同步更新 dialog/index.ts 中的两个函数返回值为新类串。

    步骤 5 — 运行全量测试（最终 GREEN 确认）：npx vitest run
  </action>
  <verify>
    <automated>npx vitest run tests/components/dialog.test.ts &amp;&amp; npx vitest run</automated>
  </verify>
  <done>dialog.test.ts 全绿。npx vitest run 全量套件通过（无回归）。DialogContent.vue 含 bottom-0、rounded-t-xl、slide-in-from-bottom，不含 top-1/2、-translate-y-1/2。</done>
</task>

</tasks>

<verification>
1. npx vitest run — 全量通过（5 个测试文件：stores/ 历史 + 本 Phase 新增 4 个组件测试）
2. 浏览器确认 Tab 指示器：切换三个 Tab，观察底部青绿线条出现/消失的 opacity 过渡（200ms）
3. 浏览器确认 Dialog：打开弹出，观察从屏幕底部滑入（200ms）+ 背景毛玻璃；关闭时向下退出（150ms）
4. 深色/浅色主题下 Tab 和 Dialog 视觉均正确
</verification>

<success_criteria>
- COMP-03: TabsTrigger.vue 含 after:bg-primary 指示线，data-[state=active] 触发 opacity 过渡，旧胶囊背景类已移除
- COMP-04: DialogContent.vue 底部定位 + slide-in-from-bottom；DialogOverlay backdrop-blur-md；无 Vue Transition 包裹
- 测试: tabs.test.ts、dialog.test.ts 全绿
- 无回归: npx vitest run 全局通过
</success_criteria>

<output>
完成后创建 `.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-02-SUMMARY.md`
</output>
