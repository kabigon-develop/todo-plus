---
phase: 02-yuan-zi-zu-jian-zhong-she-ji
plan: "04"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button/Button.vue
  - src/components/ui/button/index.ts
  - tests/components/button.test.ts
autonomous: true
requirements:
  - COMP-01
must_haves:
  truths:
    - "Button destructive 变体在浅色和深色主题下通过语义化 token 表达，不再残留硬编码 red 调色板类"
    - "批量删除和单项删除按钮继续使用 destructive 语义，但样式来源与 token 系统一致"
    - "Button 回归测试会在 destructive 变体重新出现 hardcoded palette 类时失败"
  artifacts:
    - path: "src/components/ui/button/Button.vue"
      provides: "Button 组件 destructive 变体的语义化 token 类串"
      contains: "destructive"
    - path: "src/components/ui/button/index.ts"
      provides: "供测试和调用方复用的 buttonVariants 导出，和组件 destructive 类串保持一致"
      contains: "destructive"
    - path: "tests/components/button.test.ts"
      provides: "COMP-01 destructive 变体非硬编码回归断言"
      contains: "destructive"
  key_links:
    - from: "src/components/ui/button/index.ts"
      to: "tests/components/button.test.ts"
      via: "buttonVariants import"
      pattern: "buttonVariants"
    - from: "src/components/ui/button/Button.vue"
      to: "src/components/ui/button/index.ts"
      via: "shared destructive variant contract"
      pattern: "destructive"
gap_closure: true
---

<objective>
关闭 02-VERIFICATION.md 中唯一 blocker：把 Button destructive 变体从硬编码 `bg-red-500 text-white hover:bg-red-600` 迁移为语义化 token，并补齐防回归测试。

Purpose: 收尾 COMP-01，确保 Phase 2 真正满足“所有原子 UI 组件使用语义化 Token 表达颜色”的目标。
Output: 更新后的 Button destructive variant（组件 + helper）和 destructive 回归测试。
</objective>

<execution_context>
@C:/Users/Admin/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Admin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-CONTEXT.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-RESEARCH.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-VERIFICATION.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-01-SUMMARY.md
@.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-03-SUMMARY.md
@src/components/ui/button/Button.vue
@src/components/ui/button/index.ts
@tests/components/button.test.ts

<interfaces>
From src/components/ui/button/index.ts:
```ts
export const buttonVariants = cva(
  'inline-flex items-center justify-center whitespace-nowrap rounded-md text-sm font-medium ring-offset-surface-card transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-border-focus focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',
  {
    variants: {
      variant: {
        default: 'bg-primary text-white font-semibold hover:bg-primary-hover',
        destructive: 'bg-red-500 text-white hover:bg-red-600',
        outline: 'border border-primary bg-surface-card text-primary-text hover:bg-primary-muted',
        secondary: 'bg-surface-base text-foreground hover:bg-surface-base/80',
        ghost: 'hover:bg-surface-base hover:text-foreground',
        link: 'text-foreground underline-offset-4 hover:underline'
      }
    }
  }
);
```

From tests/components/button.test.ts:
```ts
import { buttonVariants } from '../../src/components/ui/button/index';

describe('COMP-01: buttonVariants', () => {
  it('default variant contains bg-primary and text-white', () => {
    const classes = buttonVariants({ variant: 'default' });
    expect(classes).toContain('bg-primary');
    expect(classes).not.toContain('bg-foreground');
  });

  it('outline variant contains border-primary and text-primary-text', () => {
    const classes = buttonVariants({ variant: 'outline' });
    expect(classes).toContain('border-primary');
    expect(classes).toContain('text-primary-text');
  });
});
```
</interfaces>
</context>

<plan_rationale>
仅将 destructive variant 漂移视为本次 gap closure 的产品缺口，因为它直接导致 02-VERIFICATION.md 的失败 truth 与 COMP-01 未闭环。

`vite.config.ts` 扫描 `.claude/worktrees` 导致 Vitest 统计膨胀目前只是一条验证警告，不阻止 Phase 2 的用户可见目标，也不影响本次 blocker 的最小修复；它更适合作为后续测试边界治理/工具链卫生项，而不是继续扩大 Phase 2 产品收尾范围。
</plan_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 destructive 回归断言</name>
  <files>tests/components/button.test.ts</files>
  <behavior>
    - Test 1: `buttonVariants({ variant: 'destructive' })` 包含新的 destructive 语义 token 类
    - Test 2: destructive 变体不再包含 `bg-red-500`
    - Test 3: destructive 变体不再包含 `hover:bg-red-600`
  </behavior>
  <action>在 `tests/components/button.test.ts` 增加 COMP-01 destructive 用例，直接针对 `buttonVariants` 断言。测试必须锁定“语义化 token + 无 hardcoded red palette”两件事：既要断言新的 destructive token 类存在，也要显式断言 `bg-red-500` 与 `hover:bg-red-600` 不存在。不要改动既有 default/outline 断言，不要弱化测试到只检查 `text-white` 这类无关条件。</action>
  <verify>
    <automated>npx vitest run tests/components/button.test.ts</automated>
  </verify>
  <done>button.test.ts 新增 destructive 回归断言，并且在实现更新后可稳定拦截 hardcoded palette 回退。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 同步迁移 Button destructive 语义类串</name>
  <files>src/components/ui/button/Button.vue, src/components/ui/button/index.ts</files>
  <behavior>
    - Button destructive 变体使用语义化 token，而非 `bg-red-500 text-white hover:bg-red-600`
    - `Button.vue` 与 `index.ts` destructive 类串保持一致
    - App 中现有 `variant="destructive"` 调用无需改 API 即可继续渲染删除动作按钮
  </behavior>
  <action>更新 `src/components/ui/button/Button.vue` 与 `src/components/ui/button/index.ts` 的 destructive 变体，改为基于现有 Phase 2 token 体系的语义化 destructive 表达。优先复用已存在的 destructive/high-priority soft tokens（例如 `bg-[--priority-high-bg] text-[--priority-high-text]` 这一类既有 token 组合），避免新增依赖或扩散到业务层。保持 `variant` 名称仍为 `destructive`，因为这是已存在的调用契约；只修复样式来源，不改调用点，不改其他 Button 变体。</action>
  <verify>
    <automated>npx vitest run tests/components/button.test.ts && npx vitest run</automated>
  </verify>
  <done>`Button.vue` 和 `index.ts` 中 destructive 变体不再含 `bg-red-500`/`hover:bg-red-600`，button.test.ts 与全量 Vitest 均通过。</done>
</task>

</tasks>

<verification>
1. `npx vitest run tests/components/button.test.ts` 通过，且 destructive 用例覆盖语义 token 与无硬编码 palette。
2. `npx vitest run` 全量通过，证明 Button helper 改动未破坏既有组件回归测试。
3. `src/components/ui/button/Button.vue` 与 `src/components/ui/button/index.ts` 中不再出现 `bg-red-500` 或 `hover:bg-red-600`。
</verification>

<success_criteria>
- 02-VERIFICATION.md 中 Button destructive hardcoded palette blocker 被直接关闭
- COMP-01 在主工作区代码上完整满足：Button 所有变体颜色均来自语义 token
- destructive 回归测试存在且能阻止后续 palette drift
</success_criteria>

<output>
After completion, create `.planning/phases/02-yuan-zi-zu-jian-zhong-she-ji/02-04-SUMMARY.md`
</output>
